{
  "name": "dzlier-rptools",
  "version": "2.0",
  "scripts": {
    "start": "echo This runs at start; node index.js",
    "test": "mocha",
    "postinstall": "bower install",
    "heroku-prebuild": "echo This runs before Heroku installs your dependencies.",
    "heroku-postbuild": "echo This runs afterwards."
  }
}